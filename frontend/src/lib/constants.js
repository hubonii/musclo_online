/**
 * Shared constants for forms and UI components.
 */

export const SET_TYPES = ['working', 'warmup', 'dropset', 'failure'];
export const STAT_PERIODS = ['week', 'month', 'year'];

// Increment this version whenever a breaking change or feature removal occurs to force cache invalidation.
export const APP_VERSION = '1.0.6'; 


